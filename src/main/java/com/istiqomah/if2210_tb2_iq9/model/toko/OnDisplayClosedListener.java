package com.istiqomah.if2210_tb2_iq9.model.toko;

// Interface ini digunakan untuk mendefinisikan metode callback yang akan dipanggil ketika tampilan ditutup
public interface OnDisplayClosedListener {
    // Metode ini akan dipanggil ketika tampilan ditutup
    void onSceneClosed();
}